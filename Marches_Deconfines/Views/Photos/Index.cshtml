@model IEnumerable<Marches_Deconfines.Photo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.chemin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Article.libelle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.chemin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Article.libelle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.photoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.photoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.photoId })
        </td>
    </tr>
}

</table>
